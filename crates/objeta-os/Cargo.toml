[package]
name = "objeta-os"
version = "0.1.0"
edition = "2021"
description = "LKO Reflexive Runtime OS — dynamic compute allocation for LLM inference"

[dependencies]
objeta-core = { path = "../objeta-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"
